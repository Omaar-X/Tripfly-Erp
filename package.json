{
  "name": "tripfly-erp",
  "private": true,
  "version": "1.0.0",
  "description": "Railway dispatcher for the Trip Fly ERP monorepo",
  "engines": {
    "node": ">=20.19 <25",
    "npm": ">=10"
  },
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix backend run build",
    "start": "node scripts/railway-service.mjs start",
    "build:backend": "npm --prefix backend ci && npm --prefix backend run build",
    "start:backend": "npm --prefix backend start",
    "build:frontend": "npm --prefix frontend ci && npm --prefix frontend run build",
    "start:frontend": "npm --prefix frontend start"
  }
}
